name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: ghaction-cmake
      uses: lpenz/ghaction-cmake@v0.1
      with:
        dependencies: wx2.8-headers libwxgtk2.8-0 libwxgtk2.8-dev 
       
